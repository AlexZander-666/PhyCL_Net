PubMate 优化版：SCI Q4 冲刺行动指南 (v2.0)核心变更：从单纯的“补数据”转变为“构建基于物理特性的轻量化叙事”。目标：在 3 天内完成所有能让审稿人闭嘴的证据链。🚨 Day 1: 夯实数据底座 (Data Foundation)1. 启动多倍数种子训练 (Priority: Highest)不要犹豫，现在的每一分钟 GPU 空闲都是浪费。任务: 运行 Lite-AMSNet (No-MSPA) 的 Seeds 456, 789, 1024。目的: 凑齐 N=5，计算 Mean ± Std。命令:python DMC_Net_experiments.py --dataset sisfall --model amsv2 --eval-mode loso --seeds 456 789 1024 --epochs 100 --ablation mspa:False
2. 计算“硬通货”指标 (Priority: High)不要纠结于 4090 上的 ms，算出 FLOPs 才是王道。任务: 运行 scripts/calc_complexity.py。输出: 获得精确的 Params (M) 和 MACs/FLOPs (G)。对比: 找一个基线（如 InceptionTime 或 Transformer），也跑一下这个脚本，算出倍数差异（例如：我们的 FLOPs 仅为 Transformer 的 15%）。🛡️ Day 2: 构建防御逻辑 (Logic Defense)1. TFCL 的“无罪辩护”任务: 检查代码中的 TFCL 实现。确认点: 确保 TFCL 的输入是 torch.fft.rfft(x) (固定变换)，而不是依赖 MSPA 输出的特征。写作策略: 在 Method 章节明确指出：“TFCL introduces zero inference cost (computation only during training) and relies on physics-based FFT priors, not heavy learnable parameters.”2. “物理感知”消融实验任务: 整理 With MSPA vs Without MSPA 的对比表。关键点:如果 Accuracy 差不多（例如 98.05% vs 98.01%），重点强调 FLOPs 降低了 30%。结论: “对于跌倒这种冲击信号，时域的 DKS 已经足够捕获特征，频域的 MSPA 属于过度参数化（Over-parameterization）。”📊 Day 3: 可视化与补漏 (Viz & Gap Filling)1. 必杀技图表：t-SNE任务: 画出 Lite-AMSNet 最后一层的特征分布。要求: 跌倒（Fall）和跑跳（ADL）必须分得非常开。这证明了 DKS 的有效性。2. (可选但推荐) 跨数据集的“快餐版”验证任务: 如果跑不完整个 UniMiB，只跑 UniMiB 的一个子集（例如只取 Fall 和 Run/Jump 类）。话术: "To verify generalization on dynamic activities, we tested on the rigorous subset of UniMiB..." (SCI Q4 接受这种特定场景验证)。📝 交付物清单 (Checklist)模块关键指标/内容状态StatisticsN=5 Seeds, Mean > 97.5%, Std < 0.5%🔲EfficiencyParams < 1.5M, FLOPs < 1G (具体的数), FPS > 100 (4090上)🔲Narrative明确放弃 MSPA 是因为“冲击信号特性”而非“失败”🔲Visuals混淆矩阵 (N=5 聚合), t-SNE🔲