# Artifact Release Notes

## v1
- Published reviewer-facing code surface.
- Published Orange Pi CPU-only benchmark evidence.
- Published supplemental noise robustness evidence.
- Published normalized SisFall result bundles, excluding the blocked `phycl` summary.
- Published cross-dataset evidence as supportive-only supplemental material.

## Known Gaps
- Some legacy `SCI666` result summaries require normalization before publication.
- The `phycl` SisFall summary requires regeneration or provenance cleanup before it can be released as final backing evidence.
- Cross-dataset release evidence remains supportive-only until exact manuscript-table provenance is frozen.
